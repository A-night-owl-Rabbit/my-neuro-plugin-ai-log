{
  "api_url": {
    "title": "API 地址",
    "description": "用于生成日志的 LLM API 地址（留空则使用主 LLM）",
    "type": "string",
    "default": "https://api.siliconflow.cn/v1/chat/completions"
  },
  "api_key": {
    "title": "API Key",
    "description": "LLM API 密钥（留空则使用主 LLM 的密钥）",
    "type": "string",
    "default": ""
  },
  "model": {
    "title": "模型名称",
    "description": "用于生成日志的模型（留空则使用主 LLM 的模型）",
    "type": "string",
    "default": "deepseek-ai/DeepSeek-V3.2"
  },
  "thinking_mode": {
    "title": "思考模式",
    "description": "部分模型（如 DeepSeek V4）支持：enabled 开启，disabled 关闭；不支持的模型可忽略",
    "type": "string",
    "default": "enabled"
  },
  "reasoning_effort": {
    "title": "思考强度",
    "description": "部分模型支持：high 或 max；不支持的模型可忽略",
    "type": "string",
    "default": "max"
  },
  "diary_folder": {
    "title": "日志保存目录",
    "description": "AI 日志保存目录。留空则使用程序根目录下的「AI日志」文件夹（由插件自动创建）；也可填写绝对路径。",
    "type": "string",
    "default": ""
  },
  "diary_filename_template": {
    "title": "日志文件名模板",
    "description": "每日日志的文件名模板，{date} 会被替换为日期（如 2026-03-16）",
    "type": "string",
    "default": "{date}AI日志.txt"
  },
  "monthly_filename_template": {
    "title": "月度总结文件名模板",
    "description": "月度总结的文件名模板，{month} 会被替换为年月（如 2026-03）",
    "type": "string",
    "default": "{month}-月度总结.txt"
  },
  "core_memory_file": {
    "title": "核心记忆文件",
    "description": "核心用户记忆文件路径（相对于程序根目录）",
    "type": "string",
    "default": "AI记录室/核心用户记忆.txt"
  },
  "conversation_history_file": {
    "title": "对话历史文件",
    "description": "对话历史（记忆库）文件路径（相对于程序根目录）",
    "type": "string",
    "default": "AI记录室/记忆库.txt"
  },
  "history_backup_folder": {
    "title": "记忆库备份目录",
    "description": "写完日志后，将记忆库文件备份到此目录（建议绝对路径）。备份后清空原记忆库。留空则不备份。",
    "type": "string",
    "default": ""
  },
  "daily_prompt": {
    "title": "每日日志提示词",
    "description": "生成每日 AI 日志时使用的系统提示词，请按你的角色与人格自行撰写（本仓库示例不包含具体人设）",
    "type": "text",
    "default": "请根据对话历史，以你设定的角色口吻撰写一篇分板块的「观察日志」。要求：连贯段落、不使用 Markdown 列表符号；结尾有简短总结。"
  },
  "monthly_prompt": {
    "title": "月度总结提示词",
    "description": "生成月度总结时使用的系统提示词，请按你的角色与人格自行撰写",
    "type": "text",
    "default": "请根据本月所有每日日志，以你设定的角色口吻撰写月度回顾。要求：连贯段落、体现时间跨度；结尾有整体收束。"
  },
  "trigger_after_hour": {
    "title": "日志触发时间（时）",
    "description": "只有在每天几点之后才允许生成每日日志（24小时制，如 21 表示晚上9点）",
    "type": "int",
    "default": 21
  },
  "max_retries": {
    "title": "API 最大重试次数",
    "description": "调用 API 失败后的最大重试次数",
    "type": "int",
    "default": 3
  },
  "night_hour_start": {
    "title": "夜间起始时间",
    "description": "凌晨几点前算作前一天（0-7 之间的整数）",
    "type": "int",
    "default": 7
  }
}
